# ── Core (always required) ────────────────────────────────────────────────────
streamlit>=1.35.0
numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
tifffile>=2023.1
scipy>=1.11
scikit-image>=0.21
plotly>=5.18

# ── GPU acceleration (optional) ───────────────────────────────────────────────
#
# 1. CuPy — accelerates Frangi filter (~8-10x speedup, biggest win)
#    Match your CUDA version:
#      CUDA 11.x:  pip install cupy-cuda11x
#      CUDA 12.x:  pip install cupy-cuda12x
#    https://docs.cupy.dev/en/stable/install.html
#
# 2. OpenCV CUDA — accelerates CLAHE, morphology, Gaussian blur (~4-5x)
#    Standard opencv-python has no CUDA. Options:
#      a) Build from source with CUDA enabled
#      b) conda install -c conda-forge opencv  (picks up CUDA if toolkit present)
#      c) Pre-built wheels: github.com/cudawarping/opencv-python-cuda-wheels
#    Then: pip uninstall opencv-python && install the CUDA build
#
# Auto-detected at startup — no code changes needed. Toggle in sidebar.

# ── Model-based segmentation addons (optional) ───────────────────────────────
#
# "Model-based" pipeline mode (Tab 1) routes to plugins/ addons. Each addon
# is independent and only appears in the "Addon" dropdown if its deps are
# importable — the dashboard works fine with none of these installed.
#
# Cellpose — generalist nuclei/cytoplasm segmentation:
#   pip install cellpose
#
# StarDist — star-convex nuclei detection (also needs TensorFlow):
#   pip install stardist tensorflow
#
# Both can use GPU (PyTorch/TensorFlow CUDA) independently of the
# CuPy/OpenCV-CUDA GPU layer used by the manual pipeline above.
